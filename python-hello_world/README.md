0-run

Shell script
Run a Python file whose name is stored in $PYFILE.
You must use the environment variable, not hardcode the filename.

1-run_inline

Shell script
Run Python code stored inside $PYCODE.
The variable contains actual Python code, not a filename.

2-print.py

Python script
Print exactly:
"Programming is like building a multilingual puzzle
The output must match exactly, including formatting.

3-print_number.py

Python script
Print:
<number> Battery street
Use an f-string.
Do not convert the number to a string manually.

4-print_float.py

Python script
Print a float with exactly 2 decimal places.
Use f-string formatting (for example, .2f).

5-print_string.py

Python script
Print the string 3 times on one line.
Then print its first 9 characters on the next line.
No loops allowed.

6-concat.py

Python script
Use the variables str1 and str2 to print:
Welcome to Holberton School!
The program must be exactly 5 lines.

7-edges.py

Python script
Use slicing to create:

First 3 letters of word

Last 2 letters of word

The middle part (without first and last letters)

8-concat_edges.py

Python script
Use slicing only (no new variables, no string literals) to print:
object-oriented programming with Python
The program must be exactly 5 lines.

9-easter_egg.py

Python script
Print “The Zen of Python”.
There is a built-in Python trick to do this in very few characters.
